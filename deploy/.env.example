# 复制为 .env 并填写。切勿提交 .env 到版本库。

# 公网域名（Caddy用它签发TLS证书；无域名的本地测试见DEPLOY.md）
CCW_DOMAIN=ccw.example.com

# PostgreSQL密码（自定义强密码）
POSTGRES_PASSWORD=change-me-to-a-strong-password

# 令牌签名密钥：必须是64位hex（32字节）。生成：openssl rand -hex 32
CCW_TOKEN_KEY=REPLACE_WITH_openssl_rand_hex_32

# 项目容器内Claude Code版本（留空=安装最新；生产应固定，如 1.2.3）
CLAUDE_CODE_VERSION=

# 用量加权系数：input,output,cache_read,cache_write
# 把四种token折算成一个"内部额度单位"数值，闸门用它跟项目限额比较。
#
# **2026-08-02 按真机数据校准。**原值 1,5,1,1 把缓存读按输入的全价计，
# 而缓存读实际只有输入的 1/10——那正是缓存存在的意义。真机上的表现是：
# Claude 账号 5 小时窗口才用到 11%，我们的估算已经报 105% 并把项目降级为
# cleanup。长上下文会话每轮重读十几万 token 缓存，这一项以全价计入，
# 几十轮就吃光了限额，而账号那边几乎没动。
#
# 现在按公开定价的相对比例取值（以输入为 1）：
#   输入 1 · 输出 5 · 缓存读 0.1 · 缓存写 1.25
# 整数化后统一乘 10：
CCW_USAGE_WEIGHTS=10,50,1,13

# 注意：换了系数之后单位的绝对量级也变了，**限额必须一起调**，
# 否则等于换了把尺子却没换刻度。用 /admin/usage 上的真实 token 分布来定：
# 那一页按模型拆开显示输入/输出/缓存读/缓存写，乘上面的系数就是本项目的单位数。
