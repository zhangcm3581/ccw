remote-claude-workspace/

# 部署密钥与本地配置
deploy/.env
.env

# cclaude CLI 在被同步目录里生成的本地基线索引
.cclaude/

# 本地构建产物
/cclaude
/control-api
/worker-agent
/ccwadmin
/cclaude.exe
dist/

# 管理后台的设计稿。是实现的对照基准，但体积大且不参与构建，
# 只留在本地：internal/console/templates/admin_layout.html 顶部的注释指向它。
ui/
